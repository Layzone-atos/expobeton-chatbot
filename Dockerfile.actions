# Dockerfile for Action Server
FROM europe-west3-docker.pkg.dev/rasa-releases/rasa-pro/rasa-pro:3.10.27

# Set working directory
WORKDIR /app

# Copy project files
COPY actions /app/actions
COPY domain /app/domain
COPY endpoints.yml /app/

# Expose action server port
EXPOSE 5055

# Run action server on fixed port, Heroku will handle port mapping
CMD ["run", "actions", "--port", "5055"]