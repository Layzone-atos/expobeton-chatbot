# This file contains the different endpoints your bot can use.

# Server where the models are pulled from.
# https://rasa.com/docs/rasa-pro/production/model-storage#fetching-models-from-a-server

#models:
#  url: http://my-server.com/models/default_core@latest
#  wait_time_between_pulls:  10   # [optional](default: 100)

# Server which runs your custom actions.
# https://rasa.com/docs/rasa-pro/concepts/custom-actions

action_endpoint:
  url: "http://worker:5055/webhook"

# Tracker store which is used to store the conversations.
# By default the conversations are stored in memory.
# https://rasa.com/docs/rasa-pro/production/tracker-stores

#tracker_store:
#    type: redis
#    url: <host of the redis instance, e.g. localhost>
#    port: <port of your redis instance, usually 6379>
#    db: <number of your database within redis, e.g. 0>
#    password: <password used for authentication>
#    use_ssl: <whether or not the communication is encrypted, default false>

#tracker_store:
#    type: mongod
#    url: <url to your mongo instance, e.g. mongodb://localhost:27017>
#    db: <name of the db within your mongo instance, e.g. rasa>
#    username: <username used for authentication>
#    password: <password used for authentication>

# Event broker which all conversation events should be streamed to.
# https://rasa.com/docs/rasa-pro/production/event-brokers

#event_broker:
#  url: localhost
#  username: username
#  password: password
#  queue: queue

# Using Cohere (trial key - free)
# DISABLED: LLM rephraser ignores language instructions and switches to French
# nlg:
#   type: rephrase
#   llm:
#   model_group: cohere-command
#   prompt_template: prompts/rephraser_demo_personality_prompt.jinja2
#   rephrase_all: False
#   summarize_history: False

model_groups:
- id: cohere-command
  models:
  - provider: cohere
    model: command-r-plus
    timeout: 30
    max_completion_tokens: 256
- id: cohere-embed
  models:
  - provider: cohere
    model: embed-english-v3.0
    timeout: 30
# OpenAI configuration (disabled due to quota)
# - id: openai-gpt-4o
#   models:
#   - provider: openai
#     model: gpt-4o-2024-11-20
#     timeout: 7
#     max_completion_tokens: 256
# - id: openai-embeddings
#   models:
#   - provider: openai
#     model: text-embedding-3-large